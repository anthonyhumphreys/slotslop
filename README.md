# 🎰 slotslop

A Vegas-style CLI slot machine that rolls your AI coding harness, model, and effort — then runs the command.
Only harnesses with installed CLI binaries on your `PATH` are shown.

```sh
npx slotslop "Update my homepage to have dark mode"
```
